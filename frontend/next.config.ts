import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Turbopack reads path aliases from tsconfig.json automatically
  // No additional configuration needed
};

export default nextConfig;
